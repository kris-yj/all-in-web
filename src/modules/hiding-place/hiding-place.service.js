/* Automatically generated by './build/add-module.js' */
import MegHttp from 'megvii-http';
import config from '@/utils/config.utils';

// HttpUtils from insight-utils, HttpUtils.get or HttpUtils.post

// path prefix
const baseUrl = config.apiPath.service; // 基础地址
// const auth = window.config.globalConfig.apiPath.auth; // auth地址

// service path
const urls = {
  'getTrack': `${baseUrl}/api/kunlun/judge/foothold`,
  'export': `${baseUrl}/api/kunlun/judge/foothold/export`, // 导出
  'searchByPhotos': `${baseUrl}/api/kunlun/search/capture/allInOne/queryCaptureResult`, // 以图搜图
};

// service methods
const HidingPlaceSrv = {
  /**
   *  轨迹分析
   */
  getTrack(params = {}) {
    return MegHttp.httpPost(urls.getTrack, params);
  },
  // 导出
  export(params = {}) {
    return MegHttp.httpPost(urls.export, params);
  },
  searchByPhotos(params = {}) {
    return MegHttp.httpPost(urls.searchByPhotos, params);
  },
};

export default HidingPlaceSrv;
