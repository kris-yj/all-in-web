import CommonCaptureDetail from './capture-detail';

CommonCaptureDetail.install = function (Vue) {
  Vue.component(CommonCaptureDetail.name, CommonCaptureDetail);
};

export default CommonCaptureDetail;
